<div class="container">
<h2>Articles</h2>
<% if logged_in? %>
  <%= link_to 'New Article', new_user_article_path(:user_id => current_user.id) %>
<% end %>

  <% @articles.each do |article| %>
    <ul>
      <li><%= link_to article.title, article_path(article) %></li>
    </ul>
  <% end %>
</div>
